---
# A combined hash of all networks to be used within a deployment.
container_combined_networks: "{{ container_default_networks | combine(container_networks) }}"

container_fqdn: "{{ inventory_hostname }}.{{ physical_host }}.{{ container_domain }}"
