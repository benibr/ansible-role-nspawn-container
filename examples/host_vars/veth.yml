---
ansible_host: "{{ inventory_hostname }}.{{ physical_host }}.local"
ansible_become: True
ansible_user: root

physical_host: physical_host

container_networks:
  veth:
    address: "{{ ansible_host }}"
    netmask: "255.255.255.0"
    type: veth
