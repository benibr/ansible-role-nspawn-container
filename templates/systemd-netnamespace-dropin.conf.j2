[Service]
{% if exec_before %}
ExecStartPre={{ exec_before }}
{% endif %}
ExecStart=
ExecStart=/usr/bin/systemd-nspawn --quiet --keep-unit --boot --link-journal=try-guest --netns={{ use_existing_netns }} -U --settings=override --machine=%i
