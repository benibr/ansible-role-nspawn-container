---
ansible_host: 172.29.100.101
ansible_become: True
ansible_user: root

physical_host: localhost

container_name: container_macvlan
container_tech: nspawn

container_networks:
  macvlan:
    address: "{{ ansible_host }}"
    netmask: "255.255.255.0"
    interface: mw0
    type: macvlan
